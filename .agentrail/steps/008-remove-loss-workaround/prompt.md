sw-mlpl 67c2ca86 fixed upstream issues (a) u:-argument targets inside grad and (b) comparison masks. Remove the globals workaround: u:loss(inp, pos, mask, tgt) (or similar) taking arguments, drop u:set_doc's global_set of cur_*, update microgpt.mlpl's train loop, tests (model, gradcheck -- FD perturbation of param globals via global_set stays), benchmarks; optionally use infix < in u:causal_mask if (b) now accepts it inside grad. Verify: all tests pass, reg-rs output byte-identical (same losses), speed not worse (just bench). Mark (a)/(b) fixed in docs/upstream-issues.md with the sw-mlpl commit.