docs/plan.md step 5. tests/gradcheck.mlpl: central finite differences on several entries of every param matrix vs grad(u:loss(toks, tgt), W), rel tol 1e-5. Create docs/upstream-issues.md recording issues reported to sw-mlpl and their status: (a) u:-function argument lookup inside grad (cross_entropy targets, rotate shift, pow exponent, transpose_axes axes) -- confirmed, fix in progress upstream; (b) infix < rejected inside grad while lt() works; (c) stale 'tape-lowered for heads=1' line in lang-reference (4-head causal_attention trains); (d) gen_state is Model-DSL-only, by design. If (a) has landed, remove any globals workaround from microgpt.mlpl. Do not proceed to training until gradcheck passes.