Parity achieved: microgpt.mlpl --rs-parity output byte-identical to microgpt-rs (1000 loss lines, 1.9146, 20 names). lib/splitmix64 = microgpt-rs RNG in pure MLPL (16-bit limbs, counter-based vectorized draws; shuffle head via backward trace 0.73s), reusable (u:sm64_ prefix, demo-mlpl-libraries contract). Sampling now one sequential stream + rs choices rule. scripts/parity.sh + reg-rs microgpt-parity baseline. Chose pure MLPL over JSON dump tool / native extension.