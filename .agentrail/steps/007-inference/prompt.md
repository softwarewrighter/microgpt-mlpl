docs/plan.md step 7 (design decision 9). 20 samples at temperature 0.5: start from BOS, recompute forward over the prefix, last_row of logits, sample(logits, 0.5, seed) with a deterministic per-(sample,pos) seed, stop on BOS or block_size. Print '--- inference (new, hallucinated names) ---' and 'sample %2d: name' like microgpt.py. Done when outputs look like plausible names. Tests are mlplunit suites (tests/test_*.mlpl, u:assert_*). If microgpt.mlpl output changes, run `just rebaseline` and review the work/reg-rs .out diff. Gate: `just check`.