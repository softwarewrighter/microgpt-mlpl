Params init + speed. lib/params.mlpl (9 param leaves, Python shapes, randn seeds 101-109 * 0.08, num_params 4192); tests/test_params.mlpl 4 pass. Speed: lib/bench.mlpl + benchmarks/bench_setup.mlpl + scripts/bench.sh (just bench), log in docs/benchmarks.md. Found array reads copy (~0.4ms/228k read) -> added vectorized u:doc_batch (1000 docs 1.78ms vs 1.8s per-doc); sort-based vocab scan (254->19ms). run.sh 0.275->0.057s. Param list literal can't be stored in a variable (adam list must be inline). mlplbench can't set source-dir, so own helper.