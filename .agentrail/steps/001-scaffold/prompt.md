docs/plan.md step 1. Create .gitignore (input.txt, parity/out/), scripts/run.sh (locate mlpl-repl via $MLPL_REPL, PATH, else ~/github/sw-ml-study/sw-mlpl/target/release/mlpl-repl; download input.txt from the makemore names URL used by microgpt.py if missing; run microgpt.mlpl with --data-dir .), scripts/test.sh (run tests/*.mlpl, non-zero exit on failure; passes with zero tests), scripts/pre-commit.sh implementing the gate described in CLAUDE.md (ASCII-only markdown excluding agentrail-generated CLAUDE.md/AGENTS.md, test.sh, bash -n/shellcheck on scripts, cargo fmt/clippy/test + sw-checklist for any tools/* crates), and a stub microgpt.mlpl. Done when scripts/run.sh works from a clean clone and scripts/pre-commit.sh passes.