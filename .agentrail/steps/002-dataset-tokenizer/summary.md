Dataset + tokenizer. lib/data.mlpl: byte-level parse (newline spans, empty lines dropped, non-name bytes -> err), vocab via 256-way presence scan, byte->id LUT, BOS=26; u:encode/decode/doc_tokens. microgpt.mlpl prints num docs: 32033 / vocab size: 27; order = shuffle(range(N), 42). tests/test_tokenizer.mlpl 7 tests pass; reg-rs rebaselined. Notes: input.txt has NO trailing newline; include paths resolve from source_root (repo root); records work well as a dataset bundle (d.lut etc.). Split defs into lib/ so suites can include them (plan layout updated).