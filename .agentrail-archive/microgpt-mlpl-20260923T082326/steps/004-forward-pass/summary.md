Forward pass. lib/model.mlpl: whole-doc masked GPT (gather embeddings, rmsnorm via x^2@(1/16), linear x@W^T, 4 heads via constant [16,4] selectors, softmax(qk^T/2+mask), ReLU MLP, residuals, lm_head). Globals workaround still needed (u:set_doc + no-arg u:loss; repro still fails on 0.22.0 ab858695+). tests/test_model.mlpl 4 pass: loss ~ln27 (emma 0.5, 100-doc mean 0.1), softmax rows, prefix==KV-cache loop to 1e-12, blocks. Bench: set_doc 0.38ms + adam(9 params) 0.75ms/step => ~1.1s projected; eager forward 1.37ms slower than tape 0.71ms; adam returns pre-update loss.