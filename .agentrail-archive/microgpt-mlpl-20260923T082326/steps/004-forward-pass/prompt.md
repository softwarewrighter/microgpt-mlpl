docs/plan.md step 4 (design decisions 2, 4, 5). Implement rmsnorm (no gain, eps 1e-5), linear as matmul(X, transpose(W)), 4-head causal attention (reshape [n,4,4] + take per head, scale 1/sqrt(4), causal mask via lt()/gt() -- the infix < is rejected inside grad), ReLU MLP, residuals, lm_head, and u:loss(toks, tgt) using cross_entropy. Check whether sw-mlpl's fix for u:-argument targets inside grad has landed (repro in plan decision 5); if not, use the temporary globals + no-arg u:loss() workaround and note it for removal. Tests: untrained loss within 0.1 of ln(27); softmax rows sum to 1 +- 1e-9; masked whole-sequence forward equals token-by-token prefix recompute on one doc. Tests are mlplunit suites (tests/test_*.mlpl, u:assert_*). If microgpt.mlpl output changes, run `just rebaseline` and review the work/reg-rs .out diff. Gate: `just check`. Measure speed with `just bench` (add benchmarks/bench_*.mlpl for new hot paths) and log it in docs/benchmarks.md.