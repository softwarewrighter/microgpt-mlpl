Scaffold done. scripts/ (select-mlpl, select-mlplunit, fetch-data, run.sh, test.sh via mlplunit, regress.sh via reg-rs, pre-commit.sh), mlplunit.conf, justfile (run/test/regress/rebaseline/check), stub microgpt.mlpl (loads input.txt, prints 228145 chars), smoke suite tests/test_scaffold.mlpl, reg-rs baseline microgpt-run in work/reg-rs. Gate passes in a clean clone. Deviations: mlplunit + reg-rs instead of a hand-rolled harness (user request); $MLPL instead of $MLPL_REPL (ecosystem convention). Gotcha: macOS /usr/bin/grep has no -P; ASCII check uses perl. reg-rs rebase accepts the LATEST run, so just rebaseline runs first.