Gradcheck passes. tests/test_gradcheck.mlpl: 9 matrices x (3 entries + random direction) vs central FD (h=1e-5, abs1e-8+rel1e-5) via global_set perturb/restore; agreement ~3e-11; mutation (x1.0001) fails as expected; unused wte row and wpe past n are 0. docs/upstream-issues.md written with items a-i (a still open: globals workaround kept). Bench: 9 grads 5.77ms vs adam(9) 0.74ms -- adam shares one tape.