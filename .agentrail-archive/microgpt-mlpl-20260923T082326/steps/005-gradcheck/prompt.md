docs/plan.md step 5. tests/test_gradcheck.mlpl: central finite differences on several entries of every param matrix vs grad(u:loss(toks, tgt), W), rel tol 1e-5. Create docs/upstream-issues.md recording issues reported to sw-mlpl and their status: (a) u:-function argument lookup inside grad (cross_entropy targets, rotate shift, pow exponent, transpose_axes axes) -- confirmed, fix in progress upstream; (b) infix < rejected inside grad while lt() works; (c) stale 'tape-lowered for heads=1' line in lang-reference (4-head causal_attention trains); (d) gen_state is Model-DSL-only, by design; (e) perf: reading a large array value (global or record field) copies it, ~0.4 ms per read of a 228k array (docs/benchmarks.md); (f) a list of param leaves cannot be stored in a variable -- `[wte, wpe]` assigned to a name becomes an array (length mismatch error), so adam's param list must be written inline; (g) mlplunit/mlplbench: mlplbench roots --source-dir/--data-dir at each benchmark file's directory with no override (mlplunit has --source-dir), so this repo uses lib/bench.mlpl instead; also include paths resolve relative to the including file under -f; (h) perf: eager evaluation of u:gpt (1.37 ms) is slower than tape forward+backward (0.71 ms); (i) docs: adam returns the pre-update loss (useful, undocumented). Gradcheck through u:loss() (globals workaround, set via u:set_doc). If (a) has landed, remove any globals workaround from microgpt.mlpl. Do not proceed to training until gradcheck passes. Tests are mlplunit suites (tests/test_*.mlpl, u:assert_*). If microgpt.mlpl output changes, run `just rebaseline` and review the work/reg-rs .out diff. Gate: `just check`. Measure speed with `just bench` (add benchmarks/bench_*.mlpl for new hot paths) and log it in docs/benchmarks.md.