Inference done. lib/sample.mlpl: inverse-CDF sampling (random.choices style) from one seeded uniform stream, prefix recompute, stop on BOS. 20 plausible names (aline, arylin, marion...). tests/test_sample.mlpl 3 pass. Found upstream bug (j): string-valued statements in repeat/train/for bodies fail the loop (while OK) -> sampling loop uses while. sw-mlpl 67c2ca86 fixed (a)+(b); inserted step 008-remove-loss-workaround. Full program 0.72-0.74s vs rs 0.58-0.60s vs CPython 60.6s (low load).