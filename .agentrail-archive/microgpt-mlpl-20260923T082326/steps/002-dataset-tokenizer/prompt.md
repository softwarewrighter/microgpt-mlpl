docs/plan.md step 2 (design decision 8). In microgpt.mlpl: load input.txt, str_split lines, drop empties, deterministic doc order via shuffle(range(N), 42), uchars = sorted unique chars, BOS = len(uchars), 256-entry byte->id lookup table, u:encode(doc) -> [BOS] + ids + [BOS]. Assert the corpus is ASCII. Done when it prints 'num docs: 32033' and 'vocab size: 27' and tests/test_tokenizer.mlpl round-trips a name. Tests are mlplunit suites (tests/test_*.mlpl, u:assert_*). If microgpt.mlpl output changes, run `just rebaseline` and review the work/reg-rs .out diff. Gate: `just check`.