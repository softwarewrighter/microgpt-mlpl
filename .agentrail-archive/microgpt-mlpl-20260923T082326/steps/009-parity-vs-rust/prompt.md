docs/plan.md step 9. tools/rs-init-dump: small standalone Rust bin that copies microgpt-rs's Rng (src/main.rs) and reproduces its exact StateDict::init call order and doc shuffle, emitting JSON (weights by state_dict name, doc order). parity/parity.mlpl loads it via parse_json, assigns into the params, trains 1000 steps, writes losses. Done when per-step losses match microgpt-rs to ~1e-4 and the step-1000 loss ~= 1.9146. If they diverge, bisect with 1 doc / 1 step and compare gradients. Tests are mlplunit suites (tests/test_*.mlpl, u:assert_*). If microgpt.mlpl output changes, run `just rebaseline` and review the work/reg-rs .out diff. Gate: `just check`. Measure speed with `just bench` (add benchmarks/bench_*.mlpl for new hot paths) and log it in docs/benchmarks.md.